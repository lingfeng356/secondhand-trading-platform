┌──────────────────────────────────────────────────────────────────┐
│                    KNIFE4J 注解属性全解                           │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  @Api                                                           │
│  ├── tags      = 模块名称                                        │
│  └── description = 模块描述                                      │
│                                                                  │
│  @ApiOperation                                                  │
│  ├── value     = 接口简称                                        │
│  ├── notes     = 接口详细说明                                    │
│  └── httpMethod = 请求方式(GET/POST/PUT/DELETE)                  │
│                                                                  │
│  @ApiImplicitParam                                              │
│  ├── name       = 参数名                                         │
│  ├── value      = 参数说明                                       │
│  ├── required   = true/false (是否必填)                          │
│  ├── dataType   = 数据类型 (String/Long/Integer)                 │
│  ├── paramType  = 参数位置 (path/query/body/header/form)         │
│  ├── example    = 示例值                                         │
│  └── defaultValue = 默认值                                       │
│                                                                  │
│  @ApiModel                                                      │
│  ├── value       = 模型名称                                      │
│  └── description = 模型描述                                      │
│                                                                  │
│  @ApiModelProperty                                              │
│  ├── value        = 字段说明                                     │
│  ├── required     = true/false (是否必填)                        │
│  ├── example      = 示例值                                       │
│  ├── hidden       = true/false (是否隐藏)                        │
│  ├── allowableValues = 允许值范围 (如 "admin,user")              │
│  ├── dataType     = 数据类型                                     │
│  └── notes        = 补充说明                                     │
│                                                                  │
│  @ApiOperationSupport                                           │
│  ├── order       = 排序序号 (数字越小越靠前)                      │
│  └── author      = 作者                                          │
│                                                                  │
│  @ApiIgnore       → 无属性，直接隐藏                              │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘

200	成功	所有正常的响应
400	参数错误	手机号格式错误、价格不能为负数、必填项为空
401	未登录	Token缺失、Token过期、Token无效
403	无权限	修改别人的商品、删除别人的订单
404	资源不存在	商品不存在、订单不存在、用户不存在
409	资源冲突	手机号已注册、商品已收藏、订单已处理（重复操作）
413	请求体过大	上传图片超过限制（如>10MB）
415	不支持的媒体类型	请求Content-Type不是application/json
422	语义错误	参数格式正确但业务不允许（如：商品已下架不能购买、余额不足不能支付）
429	请求过多	验证码发送频率限制、接口限流
500	服务器错误	数据库异常、Redis连接失败、未知异常

图片url存list